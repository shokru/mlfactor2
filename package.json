{
  "name": "myst-site",
  "version": "1.0.0",
  "description": "MyST site deployment for Render",
  "scripts": {
    "start": "myst start --port $PORT --host 0.0.0.0",
    "build": "myst build --html",
    "dev": "myst start"
  },
  "dependencies": {
    "mystmd": "^1.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
